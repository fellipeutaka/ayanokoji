---
title: Secret
icon: Key
---

Generate a secret key for your project.
It uses the `crypto` module to generate a random secret key.
It's useful for JWT secret keys, session secret keys, and other secret keys.

## Usage

```package-install
npx ayanokoji secret
```
