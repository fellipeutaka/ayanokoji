# Narrow Compose document ownership

**Status**: accepted

Ayanokoji models only the Compose fields required for its mutations—service entries and generated local volumes. It preserves all other fields opaquely and does not validate or reconstruct the full Compose specification.
