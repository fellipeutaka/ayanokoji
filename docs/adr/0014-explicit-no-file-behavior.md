# Explicit no-file behavior

**Status**: accepted

When no recognized Compose file exists, initialization asks the user to choose a supported filename and removal reports a structured missing-file failure. The file adapter remains non-interactive.
