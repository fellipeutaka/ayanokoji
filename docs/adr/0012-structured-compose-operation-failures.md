# Structured Compose operation failures

**Status**: accepted

Compose document and file modules report structured failure categories rather than terminal-ready strings. Command modules translate discovery, read, parse, mutation, serialization, and write failures into user-facing messages and exit behavior.
